# DMS Wallpaper Wiring Cleanup Progress

## Status

Planned. Not executed yet.

## Scope

Cleanup target:
- [dms-wallpaper.nix](../../../modules/features/desktop/dms-wallpaper.nix)

Primary concerns:
- static config generated by activation instead of declarative HM config
- duplicated `awww` wrapper wiring
- package ownership readability

## Constraints

- keep `dms-awww` as a single functional integration unit
- do not split wallpaper and theme behavior into separate features
- do not move host policy into shared feature code

## Planned Validation

- `./scripts/run-validation-gates.sh structure`
- `./scripts/run-validation-gates.sh predator`
- `./scripts/check-docs-drift.sh`
- `bash scripts/check-changed-files-quality.sh`
- `nix build` before/after for `predator` system and HM
- `nix store diff-closures` before/after
