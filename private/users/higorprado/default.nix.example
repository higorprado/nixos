# Copy to ./default.nix (untracked) and adjust as needed.
# Keep this file import-only to avoid a messy private override layer.
{ ... }:
{
  imports = [
    ./env.nix
    ./git.nix
    ./paths.nix
    ./ssh.nix
    ./theme-paths.nix
  ];
}
